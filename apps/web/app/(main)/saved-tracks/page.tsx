import { Metadata } from "next";
import SavedTracksPage from "sglmps/components/saved-tracks";

export const metadata: Metadata = {
  title: "Liked | Shuffle Glimpse",
};

export default SavedTracksPage;
