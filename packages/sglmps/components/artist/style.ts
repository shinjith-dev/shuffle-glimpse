import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  artistItem: {
    gap: 12,
    alignItems: "center",
    maxWidth: 180,
  },
  artistItemAvatar: {},
  gradient: {
    width: "130%",
    height: "120%",
    position: "absolute",
    top: -20,
    right: -20,
    zIndex: -1,
  },
});

export default styles;
